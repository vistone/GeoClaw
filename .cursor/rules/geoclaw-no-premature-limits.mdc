---
description: 禁止主动加限制参数；仅用户明确要求时才加
alwaysApply: true
---

# 禁止上来就加限制

完整说明见 `DEVELOPMENT.md` §10.8。

## 原则

用户需要限制时会 **明确让你加**。未要求时，**禁止**主动加限制类设计。

## 禁止主动添加（未获用户点名）

- 额外并发上限拆分（如再拆 reheat/keepAlive/task 更小上限）
- 人为长退避 / 冷却 / 限流门禁
- 「至少 N 条才继续」之类硬门槛
- 把大量字段改成必填校验、缺一崩溃
- 为「稳健」顺手加的配额、TopN、次数封顶、开关阉割

## 允许

- 用户 **明确要求** 的限制（按要求加，不过度发挥）
- 修 bug / 实现功能所必需的最少逻辑（不是「预防性限流」）
- 配置注释说明用途（注释 ≠ 加限制）

## 做错时

立刻撤回多余限制，改回简单可用路径；不要辩解「为了安全」。

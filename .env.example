# 本地密钥模板：复制为 .env 后填写（.env 已在 .gitignore，不会进仓库）
# cp .env.example .env

# ipinfo.io API token（覆盖 config/geoclaw.yaml 里 ipinfo.token）
IPINFO_TOKEN=

# 可选：Bing Maps Key（覆盖 flightMap.bingMapsKey）
# BING_MAPS_KEY=
